import React from "react";
import ReactDOM from "react-dom/client";
import ProjectPage016 from "./ProjectPage016";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProjectPage016 />
  </React.StrictMode>
);
