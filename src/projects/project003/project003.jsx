import React from "react";
import ReactDOM from "react-dom/client";
import ProjectPage003 from "./ProjectPage003";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProjectPage003 />
  </React.StrictMode>
);
