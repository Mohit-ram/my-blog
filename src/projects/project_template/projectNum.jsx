import React from "react";
import ReactDOM from "react-dom/client";
import ProjectPageNum from "./ProjectPageNum";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProjectPageNum />
  </React.StrictMode>
);
