import React from "react";
import ReactDOM from "react-dom/client";
import ProjectPage015 from "./ProjectPage015";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProjectPage015 />
  </React.StrictMode>
);
