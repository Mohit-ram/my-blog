import React from "react";
import ReactDOM from "react-dom/client";
import ProjectPage005 from "./ProjectPage005";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProjectPage005 />
  </React.StrictMode>
);
